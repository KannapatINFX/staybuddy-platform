{
  "name": "@staybuddy/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@staybuddy/db": "workspace:*",
    "@staybuddy/observability": "workspace:*",
    "bullmq": "^6.3.2",
    "ioredis": "^6.0.0"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "typescript": "~6.0.3",
    "vitest": "^4.1.11"
  }
}
